#!/bin/bash

# Ctrl + G to easily commit all changes to the local Git Repository.
bind '"\C-g":"git add -A && git commit -v && git push \C-m"'

# Ctrl + R to search through history using fzf.
bind '"\C-r":"fzf\n"'